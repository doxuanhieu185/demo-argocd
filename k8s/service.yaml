apiVersion: v1
kind: Service
metadata:
  name: demo-static-site-service
  namespace: default
  labels:
    app: demo-static-site
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  selector:
    app: demo-static-site
